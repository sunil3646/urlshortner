# Example environment file for server
# Copy this to `.env` and fill in real values locally (do not commit).

MONGO_URI="mongodb+srv://<db_user>:<password>@cluster0.xxxxx.mongodb.net/<dbname>?retryWrites=true&w=majority"
